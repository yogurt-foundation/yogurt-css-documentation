{% if item.enable == "y" %}<span class="px-4 py-2 text-sm text-gray-100 bg-charcoal-100 rounded-full">{{ item.variant }}</span>{% endif %}{% if item.enable == "n" %}<span class="px-4 py-2 text-sm text-gray-500 bg-gray-300 rounded-full">{{ item.variant }}</span>{% endif %}